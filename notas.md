# Erro ao instalar o pacote rn-fetch-blob

Unable to resolve "lodash" from "node_modules\rn-fetch-blob\index.js"

Solução:
yarn add lodash

# Gerar o apk android

yarn expo run:android --variant=release
